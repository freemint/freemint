#
# global definitions
#
include $(top_srcdir)/../RULES

#
# private definitions
#

#
# global CFLAGS
#
INCLUDES = -I$(top_srcdir)
CFLAGS = $(INCLUDES) $(DEFINITIONS) $(MODEL) $(GENERAL) $(OPTS) $(WARN)

#
# libIO target
#
LIBIOSTMP = $(top_srcdir)/IO/libIO.a
LIBIO = -L$(top_srcdir)/IO -lIO
