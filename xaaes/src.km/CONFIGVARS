#
# global definitions
#
include $(top_srcdir)/../../CONFIGVARS

#set our own compiler-options
OPTS = -O2 -fomit-frame-pointer
#OPTS = -O3 -fomit-frame-pointer

#
# private definitions
#

#
# MiNT must be compiled with 16 bit integers (-mshort)
#
GENERAL += -mshort -g
#GENERAL += -mshort

#
# XaAES configuration
#
include $(top_srcdir)/XAAESDEFS
