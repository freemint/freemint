language: c
os: linux
dist: trusty
sudo: required

compiler:
   - m68k-atari-mint-gcc

before_install:
  - echo ${TRAVIS_PULL_REQUEST_SLUG}
  - echo ${TRAVIS_REPO_SLUG}
  - 'sh ./.install-cross-mint.sh'

script:
  - make CROSS=yes
